DeRNAseq <- function(ct, grps, mthd = 0, norm = c('TPM', 'loess'), 
                     min.count = max(8, ncol(ct)), just.stats = TRUE) {
  
  if (mthd[1] == 0) { # default methods
    
  } else if (mthd[1] == 1) { # fast methods only
    
  } else if (mthd[2] == 2) { # medium and fast methods only
    
  } else if (mthd[3] == 3) { # fast, medium, and fast mthd only
  
  } else if (mthd[4] == 4) { # all available methods
    
  } else { # selected methods only
    
  }
  
  
}